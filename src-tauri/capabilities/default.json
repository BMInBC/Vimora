{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for Vimora desktop application",
  "windows": ["main", "*"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:default",
    "fs:default"
  ]
}
